.col-md-4.col-md-offset-4
  %h3 New ticket:
  .form-group
    = simple_form_for ticket do |f|
      = render partial: 'form', locals: {f: f}
      = f.submit "Add ticket", class: 'btn btn-default btn-small'
